# Default host tuning. Override per host with a drop-in in
# /etc/systemd/system/monad-archiver.service.d/ — clear list settings first.
[Service]
AllowedCPUs=13-14
Environment="RUST_LOG=debug,h2=warn,tower=warn"
