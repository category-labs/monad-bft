# Default host tuning. Override per host with a drop-in in
# /etc/systemd/system/monad-rpc.service.d/ — clear list settings first.
[Service]
AllowedCPUs=12-15
CPUAffinity=12,13,14,15
Environment="RUST_LOG=info,monad_ipc=debug"
